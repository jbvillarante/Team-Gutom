%ul
  - @products.each do |product|
    %li
      %h1= product.name
      %img{src: product.image}
      %span= "P #{product.price}0 (#{product.stock_qty.to_i} servings available)"
      %p.ingredients= product.raw_materials.collect(&:name).join(', ')
