<ul>
<% @products.each do |product| %>
  <li>
    <h1><%= product.name %></h1>
    <img src="<%= product.image %>" />
    <%= product.raw_materials.collect(&:name).join(', ') %>
  </li>
<% end %>
</ul>